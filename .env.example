# Local development (docker-compose postgres)
DATABASE_URL=postgresql://fleet:fleet_dev_pw@localhost:5433/fleet_reliability

# When pointing the pipeline / Streamlit app at the hosted demo database
# (Neon / Supabase), copy this file to .env and replace with the real URL.
# DATABASE_URL=postgresql://<user>:<password>@<host>/<db>?sslmode=require
